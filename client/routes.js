Router.map(function() {
  this.route("search", {
    path: "/"
  });
  this.route("resultsPage", {
    path: "/results"
  });
  this.route("clients", {
    path: "/clients"
  });
  this.route("filter", {
    path: "/filter"
  });
  this.route("google", {
    path: "/google"
  });
});
